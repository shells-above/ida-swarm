#pragma once

#include "../agent/tool_system.h"
#include "swarm_agent.h"

namespace llm_re::agent {

// Forward declaration
class SwarmAgent;

// No IRC tools needed anymore - conflicts are handled automatically
// and agents don't need to broadcast messages

} // namespace llm_re::agent